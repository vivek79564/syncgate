{
  "name": "syncgate",
  "private": true,
  "version": "0.1.0",
  "description": "SyncGate — AI proposes, humans dispose. A deterministic Human-in-the-Loop CRM staging area.",
  "scripts": {
    "dev:mock": "anna-app dev --mock-llm fixtures/happy-path.jsonl",
    "dev:real": "anna-app dev",
    "dev:off": "anna-app dev --no-llm",
    "validate": "anna-app validate"
  },
  "devDependencies": {
    "@anna-ai/cli": "^0.1.22"
  }
}
